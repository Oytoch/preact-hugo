---
kind: pipeline
name: mon-pipeline

steps:
- name: hello-word
  image: busybox
  commands:
  - echo "hello world"
  - preact create oytoch/preact-hugo  test

steps:
- name: slack
  image: plugins/slack
  settings:
    webhook: 
      from_secret: slack
    channel: drone-ci
    link_names: true
    icon_url: https://i.ytimg.com/vi/V5eHeiMB4Tg/hqdefault.jpg
    template: >
      {{#success build.status}}
        build {{build.number}} réussi. Tu vas faire quoi ? Thomas
      {{else}}
        build {{build.number}} failed. Erreur :'(. <@channelname> <@someone>
      {{/success}}

